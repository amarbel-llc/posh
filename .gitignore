# Rust workspace
/target

# RFC 0007 evolutionary-predictor pilot: mephisto source bridge. The flake
# populates vendor/mephisto from the `mephisto` flake input (store path) at
# build time; locally it is a gitignored symlink to the sibling checkout for
# the dev-loop. flake.lock is the single rev pin.
/vendor

# Nix build outputs
/result
/result-*

# Editor droppings
*~

# debug-record-posht output (posh-rec captures written to the repo root)
/posht-*.castx

# `go build` in the module dir drops the binary beside its source
/posh-palette/posh-palette
